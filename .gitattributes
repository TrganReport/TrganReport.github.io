# Normalize line endings for text files
*.cs     text eol=clrf
*.sh     text eol=lf

# Treat images as binary
*.png    binary
*.jpg    binary

# Ignore docs folder when exporting
docs/*   export-ignore

# Enable Git LFS for large assets
*.psd    filter=lfs diff=lfs merge=lfs -text
